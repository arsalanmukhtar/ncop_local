# Django Settings
DJANGO_DEBUG=False
DJANGO_SETTINGS_MODULE=ncop_project.settings.staging
DEBUG=False

# Allowed Hosts (auto-detected from local IP, but can override)
DJANGO_ALLOWED_HOSTS=192.168.x.x,127.0.0.1,localhost

# Vite
VITE_DEV_MODE=False
VITE_FRONTEND_URL=http://192.168.x.x:8080

# Database (Staging)
POSTGRES_ENGINE=django.db.backends.postgresql
POSTGRES_DB_STAGING=ncop
POSTGRES_HOST_STAGING=localhost
POSTGRES_PORT_STAGING=5432
POSTGRES_USER_STAGING=postgres
POSTGRES_PASSWORD_STAGING=postgres

# Security
SECURE_SSL_REDIRECT=False
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False

# CORS
CORS_ALLOWED_ORIGINS=http://192.168.x.x:8080,http://192.168.x.x:5173,http://127.0.0.1:8080

# API Keys
MAPBOX_ACCESS_TOKEN=pk.eyJ1IjoibmVvYzIwMjMiLCJhIjoiY2x1aTBubHAwMjYzOTJqcGc2cGFhMHU2ciJ9.tFU9_qFCY02qCvfiPMPDUg
METEOBLUE_API_KEY=1c9f9d57164f
WAQI_API_TOKEN=f23f89c8332a95d8512a6d60aa221c06e199a9eb

# Logging
LOG_LEVEL=INFO

# ===== Additional variables from .env (not already present above) =====

# Django secret key (for dev, override as needed for staging)
DJANGO_SECRET_KEY=stage-insecure

# Vite dev server (for local dev, not used in production/staging)
VITE_DEV_SERVER_HOST=localhost
VITE_DEV_SERVER_PORT=5173
VITE_HMR_HOST=localhost

# Meteoblue API variables (already present as METEOBLUE_API_KEY, but adding for completeness)
METEOBLUE_TOKEN=1c9f9d57164f

# PostgreSQL variables (already mapped above, but adding for completeness)
POSTGRES_DB=ncop
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=localhost
POSTGRES_PORT=5432